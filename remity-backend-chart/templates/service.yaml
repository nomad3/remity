apiVersion: v1
kind: Service
metadata:
  name: {{ include "remity-backend.fullname" . }}
  labels:
    {{- include "remity-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "remity-backend.selectorLabels" . | nindent 4 }}
