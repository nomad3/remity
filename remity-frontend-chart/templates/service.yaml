apiVersion: v1
kind: Service
metadata:
  name: {{ include "remity-frontend.fullname" . }}
  labels:
    {{- include "remity-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "remity-frontend.selectorLabels" . | nindent 4 }}
