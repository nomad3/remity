# Environment variables for Docker Compose variable substitution
# These are needed for the backend service's command directive.
# Values should match those in backend/.env

POSTGRES_USER=remityuser
POSTGRES_PASSWORD=remitypassword
POSTGRES_DB=remitydb
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Other variables like JWT_SECRET_KEY, API keys etc. are NOT needed here,
# as they are loaded directly into the backend container via its own env_file.
